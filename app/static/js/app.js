const newProjectButton = document.querySelector("#new-project-button");

newProjectButton?.addEventListener("click", () => {
    window.alert(
        "O módulo de criação de projetos será implementado em breve."
    );
});